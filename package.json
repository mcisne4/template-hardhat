{
  "name": "hardhat-template",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "yarn hardhat clean",
    "compile": "yarn hardhat compile",
    "node": "yarn hardhat node",
    "test": "yarn hardhat test",
    "deploy": "yarn hardhat deploy",
    "nodemon-test": "nodemon --watch 'test/**/*.js' --exec 'yarn hardhat test'",
    "recompile": "yarn hardhat watch recompile",
    "retest": "yarn hardhat watch retest",
    "redeploy": "yarn hardhat watch redeploy"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "npm:hardhat-deploy-ethers",
    "@nomiclabs/hardhat-waffle": "^2.0.3",
    "chai": "^4.3.6",
    "dotenv": "^16.0.1",
    "ethereum-waffle": "^3.4.4",
    "ethers": "^5.6.6",
    "hardhat": "^2.9.5",
    "hardhat-deploy": "^0.11.5",
    "hardhat-watcher": "^2.3.0",
    "prettier": "^2.6.2",
    "prettier-plugin-solidity": "^1.0.0-beta.19"
  }
}
